{% extends 'base.html.twig' %}

{% block title %}Edit Site{% endblock %}

{% block body %}
    <section id="trouveSpot-section1">
        <div>
            <h1>Edit Site</h1>
        </div>
    </section>

{# {% for image in site.media %}
    <div>
        <img src="{{ asset('/uploads/' ~ image )}}" alt="media" width="250px" heigth="260">
    </div>
{% endfor %} #}

    {{ include('site/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('site_index') }}">back to list</a>

    {{ include('site/_delete_form.html.twig') }}
{% endblock %}
